fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlmodel==0.0.22
pydantic<2.12  # sqlmodel 0.0.22 는 pydantic 2.12+ 와 비호환 (Field 'id' requires a type annotation)
httpx==0.28.1
openai==1.59.6
python-dotenv==1.0.1
pytest==8.3.4
# 문서 기반 수집이 UploadFile 을 쓴다. 없으면 FastAPI 가 기동 시
# RuntimeError 를 내고 테스트 수집조차 되지 않는다.
python-multipart==0.0.32
pypdf==5.1.0
truststore==0.10.0
