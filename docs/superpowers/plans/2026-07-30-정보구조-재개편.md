# 정보구조 재개편 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 프로젝트를 작업의 중심으로 되돌리고, 수집 엔진을 최상위 장소에서 참고 자료로 격하한다.

**Architecture:** 엔진은 백엔드에서 이미 `RecordingSession.kind` — 수집 사건의 속성이다. 화면도 그렇게 맞춘다. 프로젝트 목록이 각 프로젝트의 엔진·세션·액션을 보여주고(배지), 수집 시작은 프로젝트 안에서 팝업으로 엔진을 골라 그 엔진의 실제 시작 지점으로 데려간다. 팝업은 **세션을 만들지 않는다** — 세션은 실제 수집이 만든다. `/sources` 는 설명 전용으로 남는다.

**Tech Stack:** FastAPI + SQLModel + SQLite (백엔드), React 18 + Vite + react-router-dom (관리자)

## 배경 — 무엇을 고치는가

프로젝트 목록이 **이름 · ID · 삭제**만 보여준다. 내 작업을 담는 화면이 내 작업에 대해
아무것도 답하지 않는다. 그래서 "수집 과정이 안 보인다"는 문제가 나왔을 때 보이게 할 곳이
없어, 엔진을 최상위 장소로 승격시켰다. 문제는 가시성인데 처방이 정보구조 변경이었다.

그 결과로 증상 네 개가 한 원인에서 나왔다:

1. 프로젝트가 필요한 일괄 수집 폼이 프로젝트 없는 페이지(`/sources`)에 놓여, `수집 결과를
   담을 프로젝트` 드롭다운으로 우회하고 있다
2. `SourceList.tsx` 가 이름으로 프로젝트를 짐작한다 (`name.includes("공공데이터포털")`)
3. 그 폼 때문에 포털 카드만 높아지고, `.method-sources { margin-top: auto }` +
   `.method-grid { align-items: stretch }` 가 맞물려 트래픽 카드에 큰 공백이 생긴다
4. 카드가 길어져 산출물 블록이 접힘 아래로 내려갔다

**배지가 원래 문제를 해결한다.** 프로젝트 목록에서 각 프로젝트가 어떤 방식으로 모였는지
보이면 엔진을 최상위로 올릴 이유가 사라진다.

## 하지 않는 것 (YAGNI)

- **빈 세션을 먼저 만들지 않는다.** 세션 생성 시점을 바꾸면 `비어 있음` 상태가 새로 생겨
  화면마다 설명해야 하고, `RecordingSession` 컬럼 추가는 마이그레이션이 없어 `dev.db` 를
  깨뜨린다. 얻는 것이 없다.
- **`/` 를 `/sources` 로 리다이렉트하지 않는다.** 폼이 빠진 `/sources` 는 동작이 없다.
  동작 없는 화면을 첫 화면으로 두면 매번 통과만 하게 된다.
- **`/sources` 에 산출물 숫자를 두지 않는다.** 프로젝트 목록 배지와 범위가 달라
  (전체 합산 vs 프로젝트 하나) 두 숫자가 어긋나면 사용자는 버그로 읽는다. 링크만 남긴다.
- **백엔드 스키마를 바꾸지 않는다.** 새 컬럼도 새 테이블도 없다.
- `POST /api/projects` 는 이미 get-or-create 로 있다 (`sessions.py:18`). 새로 만들지 않는다.

## Global Constraints

- Python 3.10.11 — 3.11+ 문법 금지 (`match`, `except*`, `X | Y` 런타임 사용 금지)
- Node v25.8.0, npm workspaces. Docker·pnpm·uv 를 쓰지 않는다
- **새 프런트엔드 의존성을 들이지 않는다.** CSS 는 `apps/admin/src/styles/app.css` 한 파일
- **`window.confirm`·`alert`·`prompt` 를 쓰지 않는다** — 자동화를 막고 촬영 화면에서 튄다
- 주석·UI 문구·커밋 메시지·문서는 한국어
- **화면에 뜨는 숫자와 문구는 실제 데이터로 뒷받침되어야 한다.** 더미 패널 금지
- 응답 본문 원문을 저장하지 않는다 (구조 + 샘플 1건)
- `MIN_INTERVAL_SEC = 1.0` 을 낮추지 않는다
- `apps/backend/.env` 와 `.mcp.json` 을 커밋하지 않는다
- 관리자 타입체크는 **`npx tsc -b`** — `tsc --noEmit` 은 파일 0개를 검사하고 통과한다
- 백엔드 재시작은 `lsof -ti tcp:8000 -sTCP:LISTEN | xargs kill` — **`-sTCP:LISTEN` 을 빼면
  Chrome 이 함께 죽는다**

## 검증 명령

```bash
cd apps/backend && .venv/bin/pytest tests/ -q          # 현재 133 통과
cd apps/admin   && npx tsc -b --force                  # 0 이어야 함
cd apps/admin   && npm run build                       # tsc -b && vite build
```

## File Structure

| 파일 | 책임 | 작업 |
|---|---|---|
| `apps/backend/app/routers/sessions.py` | `GET /api/projects` 에 프로젝트별 요약 추가 | 수정 |
| `apps/backend/tests/test_sessions.py` | 요약 필드 테스트 | 수정 |
| `apps/admin/src/pages/ProjectList.tsx` | 배지·세션·액션·마지막 수집 컬럼, 새 프로젝트 | 수정 |
| `apps/admin/src/components/PortalCrawlPanel.tsx` | 프로젝트 고정 모드 | 수정 |
| `apps/admin/src/components/CollectStartModal.tsx` | 엔진 선택 팝업 | **생성** |
| `apps/admin/src/pages/SessionList.tsx` | `+ 수집 시작` 버튼과 팝업 연결 | 수정 |
| `apps/admin/src/pages/SourceList.tsx` | 폼·산출물 제거, 엔진 목록 링크만 | 수정 |
| `apps/admin/src/components/Shell.tsx` | 사이드바 순서 | 수정 |
| `apps/admin/src/styles/app.css` | 배지행·팝업·컬럼 | 수정 |
| `docs/demo-script.md`, `README.md`, `CLAUDE.md` | 바뀐 흐름 반영 | 수정 |

---

### Task 1: 프로젝트별 요약을 `GET /api/projects` 에 싣는다

**Files:**
- Modify: `apps/backend/app/routers/sessions.py:34-37`
- Test: `apps/backend/tests/test_sessions.py`

**Interfaces:**
- Consumes: `Project`, `RecordingSession`, `Action` — 전부 `sessions.py:7` 에 이미 임포트되어 있다. 추가 임포트가 필요 없다.
- Produces: `GET /api/projects` 가 기존 `{id, name}` 에 네 필드를 **더한** 응답을 준다:

```json
{"id": 5, "name": "포털 일괄 수집 테스트",
 "kinds": ["portal"], "sessions": 2, "actions": 10,
 "lastCollectedAt": "2026-07-29T14:03:11"}
```

기존 필드를 지우거나 이름을 바꾸지 않는다. `ActionEdit.tsx:73` 과
`PortalCrawlPanel.tsx` 가 `{id, name}` 만 읽으므로 그대로 동작한다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`apps/backend/tests/test_sessions.py` 맨 아래에 붙인다. 이 파일의 기존 픽스처
(`client`, `db_session`) 이름을 그대로 쓴다 — 파일 앞부분을 먼저 읽어 픽스처 이름과
프로젝트·세션 생성 방식을 확인하고 그 방식을 따른다.

```python
def test_프로젝트_목록이_엔진과_개수를_함께_준다(client):
    """프로젝트 목록이 이름만 주면 내 작업에 대해 아무것도 답하지 못한다."""
    rows = client.get("/api/projects").json()
    assert rows, "시드 프로젝트가 있어야 한다"
    for row in rows:
        # 기존 필드는 그대로 — 이 응답을 읽는 화면이 이미 셋 있다
        assert "id" in row and "name" in row
        assert isinstance(row["kinds"], list)
        assert isinstance(row["sessions"], int)
        assert isinstance(row["actions"], int)
        assert "lastCollectedAt" in row


def test_배지_순서는_호출마다_같다(client):
    """set 을 그대로 내보내면 순서가 흔들려 배지가 매번 자리를 바꾼다."""
    first = client.get("/api/projects").json()
    second = client.get("/api/projects").json()
    assert [r["kinds"] for r in first] == [r["kinds"] for r in second]
    for row in first:
        assert row["kinds"] == [k for k in ("traffic", "portal", "document") if k in row["kinds"]]


def test_세션이_없는_프로젝트는_빈_배지와_None_을_준다(client):
    body = client.post("/api/projects", json={"name": "요약테스트-빈프로젝트"}).json()
    rows = client.get("/api/projects").json()
    row = next(r for r in rows if r["id"] == body["id"])
    assert row["kinds"] == []
    assert row["sessions"] == 0
    assert row["actions"] == 0
    assert row["lastCollectedAt"] is None
```

- [ ] **Step 2: 실패를 확인한다**

Run: `cd apps/backend && .venv/bin/pytest tests/test_sessions.py -k 요약 -v`
Expected: `KeyError: 'kinds'` 로 FAIL

- [ ] **Step 3: 구현한다**

`sessions.py:34-37` 의 `list_projects` 를 통째로 바꾼다:

```python
@router.get("/api/projects")
def list_projects(db: Session = Depends(get_session)) -> list:
    """프로젝트 목록. 이름만으로는 내 작업에 대해 아무것도 답하지 못한다.

    엔진은 프로젝트의 속성이 아니라 그 안에서 일어난 수집 사건의 속성
    (RecordingSession.kind)이다. 그래서 프로젝트를 엔진별로 쪼개지 않고,
    한 프로젝트에 어떤 방식들이 섞여 있는지를 배지로 보여준다.
    """
    rows = db.exec(select(Project).order_by(Project.id)).all()
    out = []
    for p in rows:
        sessions = db.exec(
            select(RecordingSession).where(RecordingSession.project_id == p.id)
        ).all()
        action_count = len(
            db.exec(select(Action).where(Action.project_id == p.id)).all()
        )
        present = {s.kind for s in sessions}
        # 고정 순서로 거른다. set 을 그대로 내보내면 순서가 흔들려
        # 배지가 호출마다 자리를 바꾼다.
        kinds = [k for k in ("traffic", "portal", "document") if k in present]
        started = [s.started_at for s in sessions if s.started_at]
        out.append({
            "id": p.id,
            "name": p.name,
            "kinds": kinds,
            "sessions": len(sessions),
            "actions": action_count,
            "lastCollectedAt": max(started) if started else None,
        })
    return out
```

- [ ] **Step 4: 통과와 회귀 없음을 확인한다**

Run: `cd apps/backend && .venv/bin/pytest tests/ -q`
Expected: 기존 133 + 신규 3 = **136 통과**

- [ ] **Step 5: 커밋**

```bash
git add apps/backend/app/routers/sessions.py apps/backend/tests/test_sessions.py
git commit -m "프로젝트 목록에 엔진·세션·액션 요약을 싣는다"
```

---

### Task 2: 프로젝트 목록이 작업 상태를 보여준다

**Files:**
- Modify: `apps/admin/src/pages/ProjectList.tsx`
- Modify: `apps/admin/src/styles/app.css`

**Interfaces:**
- Consumes: Task 1 의 `GET /api/projects` 응답 (`kinds`, `sessions`, `actions`, `lastCollectedAt`)
- Consumes: `components/CollectionMark.tsx` 의 `KindMark({ kind, size })` 와
  `KIND_LABEL: Record<CollectionKind, string>` — 이미 export 되어 있다
- Consumes: `POST /api/projects` (get-or-create, 이미 존재)
- Produces: 없음 (화면 끝단)

- [ ] **Step 1: 타입과 조회를 넓힌다**

`ProjectList.tsx` 상단의 프로젝트 타입에 네 필드를 더한다:

```tsx
type Project = {
  id: number;
  name: string;
  kinds: string[];
  sessions: number;
  actions: number;
  lastCollectedAt: string | null;
};
```

- [ ] **Step 2: 배지 줄과 시각 표기를 만든다**

같은 파일 안에 둔다. 다른 화면이 쓰지 않으므로 파일을 새로 만들지 않는다.

```tsx
/** 이 프로젝트에 섞여 있는 수집 방식. 없으면 아무것도 그리지 않는다 —
 *  "없음" 배지는 자리만 차지하고 아무 것도 알려주지 않는다. */
function KindBadges({ kinds }: { kinds: string[] }) {
  if (!kinds.length) return null;
  return (
    <span className="kind-row">
      {kinds.map((kind) => (
        <span key={kind} className={`kind-chip kind-${kind}`}>
          <KindMark kind={kind} size={11} />
          {KIND_LABEL[kind as CollectionKind] ?? kind}
        </span>
      ))}
    </span>
  );
}

/** "2026-07-29 14:03" 까지만. 초는 목록에서 읽는 사람에게 쓸모가 없다. */
function formatCollected(value: string | null): string {
  if (!value) return "—";
  return value.replace("T", " ").slice(0, 16);
}
```

`CollectionMark` 에서 `KindMark`, `KIND_LABEL`, `CollectionKind` 를 임포트한다.

- [ ] **Step 3: 표를 5열로 바꾼다**

**`table-5col` 을 재사용하지 않는다.** 그 클래스는
`2fr 100px 120px 120px 180px` 이고, `.table-row .mono` 에 `text-overflow: ellipsis` 가
걸려 있어 `2026-07-29 14:03` 이 120px 안에서 **조용히 잘린다**. 삭제 열도 인라인 확인
문구("세션·액션까지 지울까요?" + 버튼 둘)가 들어가는데 기존 `table-3col` 은 그 열에
220px 를 쓴다. 전용 클래스 `table-projects` 를 만들어 쓴다 (Step 6 에서 정의).

`ID` 컬럼을 없애고 `#5` 를 이름 옆 작은 글씨로 옮긴다 — 별도 컬럼을 차지할 만한
정보가 아니지만, 대화에서 `#5` 로 부르므로 지우지는 않는다.

감싸는 div 는 `<div className="project-table table-projects">` 로 바꾼다.

```tsx
<div className="table-head">
  <span>프로젝트</span>
  <span>수집 세션</span>
  <span>액션</span>
  <span>마지막 수집</span>
  <span />
</div>
```

각 행:

```tsx
<div className="table-row" key={project.id}>
  <span>
    <i className={`project-icon icon-${i % 3}`}>{String(i + 1).padStart(2, "0")}</i>
    <Link to={`/projects/${project.id}`}><b>{project.name}</b></Link>
    <small className="mono">#{project.id}</small>
    <KindBadges kinds={project.kinds} />
  </span>
  <span className="mono">{project.sessions}</span>
  <span className="mono">{project.actions}</span>
  <span className="mono">{formatCollected(project.lastCollectedAt)}</span>
  <span>
    {/* 삭제 확인은 인라인이다. window.confirm 은 촬영 화면에서 튀고 자동화를 막는다 */}
    {confirming === project.id ? ( /* 기존 코드 그대로 */ ) : (
      <button onClick={() => setConfirming(project.id)}>삭제</button>
    )}
  </span>
</div>
```

기존 `confirming` 상태와 `remove()` 는 그대로 둔다.

- [ ] **Step 4: 새 프로젝트 만들기를 붙인다**

프로젝트가 작업의 시작점이 되었으므로 관리자에서 만들 수 있어야 한다. 없으면 첫
사용자는 빈 목록에서 확장으로 나가야 하고, 그게 지금 고치는 종류의 막다른 골목이다.

`heading-row` 안에 인라인 폼을 둔다. 팝업을 쓰지 않는다 — 입력 하나에 팝업은 과하다.

```tsx
const [newName, setNewName] = useState("");
const [creating, setCreating] = useState(false);

async function create() {
  const name = newName.trim();
  if (!name || creating) return;
  setCreating(true);
  setError(null);
  try {
    // get-or-create 다. 같은 이름이면 기존 프로젝트로 들어간다.
    const row = await api.post("/api/projects", { name });
    setNewName("");
    navigate(`/projects/${row.id}`);
  } catch (err) {
    setError(errorMessage(err));
  } finally {
    setCreating(false);
  }
}
```

입력란에 `onKeyDown={(e) => e.key === "Enter" && create()}` 를 붙인다.
`useNavigate` 를 임포트한다.

- [ ] **Step 5: 빈 상태 문구를 고친다**

지금은 "확장 사이드 패널에서 프로젝트 이름을 입력하고 기록을 시작하면 여기에
나타납니다" — 관리자에서 만들 수 있게 되었으니 거짓이 된다.

```tsx
<div className="empty-state">
  <strong>아직 프로젝트가 없습니다</strong>
  <p>
    위에서 프로젝트를 하나 만들고, 그 안에서 <strong>수집 시작</strong>을 누르세요.
    <br />
    확장 사이드 패널에서 기록을 시작해도 프로젝트가 자동으로 만들어집니다.
  </p>
</div>
```

- [ ] **Step 6: CSS 를 더한다**

`app.css` 끝에 붙인다. 기존 `--line`, `--muted`, `--ink` 변수를 쓴다.

```css
/* 프로젝트 목록 전용 그리드. table-5col 을 쓰면 '마지막 수집' 이 120px 에서
   ellipsis 로 잘리고(.table-row .mono), 삭제 열의 인라인 확인 문구가 좁아 접힌다. */
.table-projects .table-head, .table-projects .table-row {
  grid-template-columns: 2fr 90px 80px 150px 230px;
}

/* 프로젝트 목록의 엔진 배지 — 이 프로젝트가 어떤 방식으로 모였는지 */
.kind-row { display: inline-flex; gap: 4px; margin-left: 8px; vertical-align: middle; }
.kind-chip { display: inline-flex; align-items: center; gap: 3px; padding: 1px 6px;
             border-radius: 999px; font-size: 10.5px; font-weight: 650;
             border: 1px solid var(--line); background: #fff; color: var(--muted); }
.kind-chip.kind-traffic { border-color: #b7d3e3; background: #eef6fb; color: #1d5a7a; }
.kind-chip.kind-portal  { border-color: #b7e3d8; background: #f2fbf8; color: #0b6e5c; }
.kind-chip.kind-document{ border-color: #e8dcc0; background: #fdf9ef; color: #8a6d29; }

/* 새 프로젝트 인라인 폼 */
.new-project { display: flex; gap: 8px; align-items: center; }
.new-project input { padding: 8px 10px; min-width: 220px; }
```

- [ ] **Step 7: 타입체크와 눈으로 확인**

Run: `cd apps/admin && npx tsc -b --force`
Expected: exit 0, 출력 없음

Run: `cd apps/admin && npm run dev` 후 `http://localhost:5173/` 를 열어 확인한다:
- 시드 프로젝트마다 배지·세션·액션·마지막 수집이 실제 값으로 보인다
- 세션이 없는 프로젝트는 배지가 없고 마지막 수집이 `—`
- 프로젝트를 하나 만들면 그 프로젝트 화면으로 넘어간다

- [ ] **Step 8: 커밋**

```bash
git add apps/admin/src/pages/ProjectList.tsx apps/admin/src/styles/app.css
git commit -m "프로젝트 목록이 엔진 배지와 작업 개수를 보여준다"
```

---

### Task 3: 일괄 수집 폼에 프로젝트 고정 모드를 만든다

**Files:**
- Modify: `apps/admin/src/components/PortalCrawlPanel.tsx:122-134`

**Interfaces:**
- 기존 props 를 그대로 둔다: `{ projectId: number | null; onProjectChange?: (id: number) => void }`
- `onProjectChange` 가 **없으면 고정 모드**다. 새 prop 을 만들지 않는다 — 이미 optional 이고,
  "바꿀 방법을 주지 않았다" 가 곧 "고정" 이다. Task 4 가 이 모드로 쓴다.
- 고정 모드에서는 `projects` 조회가 필요 없지만, 프로젝트 **이름**을 보여주려면 필요하다.
  기존 조회를 그대로 두고 이름만 찾아 쓴다.

- [ ] **Step 1: 드롭다운을 갈라낸다**

`122-134` 의 `.crawl-target` 블록을 바꾼다:

```tsx
<div className="crawl-target">
  <label htmlFor="crawl-project">수집 결과를 담을 프로젝트</label>
  {onProjectChange ? (
    <select
      id="crawl-project"
      value={projectId ?? ""}
      onChange={(e) => onProjectChange(Number(e.target.value))}
      disabled={!!job && job.status === "running"}
    >
      {projects.map((project) => (
        <option key={project.id} value={project.id}>{project.name}</option>
      ))}
    </select>
  ) : (
    /* 프로젝트 안에서 열렸다. 고를 여지가 없으므로 드롭다운을 두지 않는다 —
       선택지가 하나인 드롭다운은 고민할 것이 있다고 잘못 알린다. */
    <strong className="crawl-fixed-project">
      {projects.find((p) => p.id === projectId)?.name ?? `#${projectId}`}
    </strong>
  )}
</div>
```

- [ ] **Step 2: CSS 를 더한다**

```css
.crawl-fixed-project { font-size: 12.5px; font-weight: 650; color: var(--ink); }
```

- [ ] **Step 3: 타입체크**

Run: `cd apps/admin && npx tsc -b --force`
Expected: exit 0

`SourceList.tsx` 는 아직 `onProjectChange` 를 넘기고 있어 드롭다운이 그대로 보인다.
Task 5 에서 그 호출 자체가 없어진다. 이 태스크만으로는 화면이 바뀌지 않는 것이 정상이다.

- [ ] **Step 4: 커밋**

```bash
git add apps/admin/src/components/PortalCrawlPanel.tsx apps/admin/src/styles/app.css
git commit -m "일괄 수집 폼에 프로젝트 고정 모드를 더한다"
```

---

### Task 4: 프로젝트 안에서 수집을 시작한다

**Files:**
- Create: `apps/admin/src/components/CollectStartModal.tsx`
- Modify: `apps/admin/src/pages/SessionList.tsx`
- Modify: `apps/admin/src/styles/app.css`

**Interfaces:**
- Consumes: Task 3 의 고정 모드 — `<PortalCrawlPanel projectId={projectId} />` 로 부른다
  (`onProjectChange` 를 넘기지 **않는다**)
- Consumes: `CollectionMark` 의 `KindMark`
- Produces:
  ```tsx
  export default function CollectStartModal(props: {
    projectId: number;
    projectName: string;
    onClose: () => void;
  }): JSX.Element
  ```

**설계 근거 — 팝업은 세션을 만들지 않는다.** 셋 중 관리자에서 시작할 수 있는 것은 포털
일괄 수집 하나뿐이다. 트래픽과 포털 명세 수집은 크롬 확장에서 일어난다. 팝업이 그걸
숨기지 않고 각 엔진의 실제 시작 지점으로 데려간다. 빈 세션을 만들면 "만들었는데 아무것도
없는 것" 이 생기고, 그건 지금 없는 문제다.

- [ ] **Step 1: 팝업을 만든다**

```tsx
import { useEffect, useState } from "react";
import PortalCrawlPanel from "./PortalCrawlPanel";
import { KindMark } from "./CollectionMark";

/**
 * 프로젝트 안에서 수집을 시작한다.
 *
 * 세션을 만들지 않는다 — 세션은 실제 수집이 만든다. 이 팝업이 하는 일은
 * 고른 엔진의 **시작 지점**으로 데려가는 것이다. 셋 중 관리자에서 바로
 * 시작할 수 있는 건 포털 일괄 수집 하나이고, 나머지 둘은 크롬 확장에서
 * 일어난다. 그 사실을 감추지 않는다.
 */
type Engine = "traffic" | "portal" | "document";

export default function CollectStartModal({
  projectId,
  projectName,
  onClose,
}: {
  projectId: number;
  projectName: string;
  onClose: () => void;
}) {
  const [engine, setEngine] = useState<Engine>("portal");

  // Esc 로 닫힌다. 팝업을 닫는 방법이 하나뿐이면 갇힌 것처럼 느껴진다.
  useEffect(() => {
    function onKey(e: KeyboardEvent) {
      if (e.key === "Escape") onClose();
    }
    window.addEventListener("keydown", onKey);
    return () => window.removeEventListener("keydown", onKey);
  }, [onClose]);

  return (
    <div className="modal-backdrop" onClick={onClose}>
      {/* 안쪽 클릭이 배경까지 올라가면 폼을 채우다 닫힌다 */}
      <div className="modal-card" onClick={(e) => e.stopPropagation()}>
        <div className="modal-head">
          <div>
            <strong>수집 시작</strong>
            <span>{projectName} 에 담습니다</span>
          </div>
          <button onClick={onClose} aria-label="닫기">✕</button>
        </div>

        <div className="engine-picker">
          {([
            { key: "traffic", label: "트래픽 기반", hint: "화면이 부르는 호출 관측" },
            { key: "portal", label: "포털 공개 기반", hint: "포털이 공개한 명세 읽기" },
            { key: "document", label: "문서 기반", hint: "준비중" },
          ] as const).map((item) => (
            <button
              key={item.key}
              className={engine === item.key ? "on" : ""}
              disabled={item.key === "document"}
              onClick={() => setEngine(item.key)}
            >
              <KindMark kind={item.key} size={16} />
              <b>{item.label}</b>
              <span>{item.hint}</span>
            </button>
          ))}
        </div>

        <div className="engine-body">
          {engine === "traffic" && (
            <ol className="guide-steps">
              <li>대상 사이트를 열고 확장 아이콘을 눌러 사이드 패널을 엽니다</li>
              <li>프로젝트 이름에 <strong>{projectName}</strong> 을 그대로 넣습니다 —
                  같은 이름이면 이 프로젝트에 담깁니다</li>
              <li>버튼·필터처럼 <strong>페이지가 넘어가지 않는 조작</strong>을 클릭합니다</li>
              <li><strong>기록 종료 및 전송</strong> 후 이 화면을 새로고침하면 세션이 보입니다</li>
            </ol>
          )}

          {engine === "portal" && (
            <>
              {/* 프로젝트 안에서 열렸으므로 프로젝트를 고를 필요가 없다 */}
              <PortalCrawlPanel projectId={projectId} />
              <p className="guide-note">
                <strong>한 건씩 모으려면 확장을 씁니다</strong>
                공공데이터포털 오픈API <strong>상세페이지</strong>(<code>요청주소</code>와
                <code>요청변수</code> 표가 함께 있는 화면)에서 사이드 패널을 열고
                프로젝트 이름에 <strong>{projectName}</strong> 을 넣어 수집하세요.
              </p>
            </>
          )}
        </div>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: 세션 목록에 버튼을 붙인다**

`SessionList.tsx:79-85` 의 `heading-row` 를 고친다. `projectName` 은 이 파일이 이미
브레드크럼용으로 들고 있다 — 없으면 `notFound` 처리 근처의 조회를 확인해 재사용한다.

```tsx
const [starting, setStarting] = useState(false);
```

```tsx
<section className="heading-row">
  <div>
    <p className="eyebrow">COLLECTION SESSIONS</p>
    <h1>수집 세션</h1>
    <p className="subtitle">트래픽 기반·포털 공개 기반 수집이 한 목록에 모입니다.</p>
  </div>
  {!notFound && (
    <button className="primary" onClick={() => setStarting(true)}>+ 수집 시작</button>
  )}
</section>

{starting && projectId != null && (
  <CollectStartModal
    projectId={projectId}
    projectName={projectName}
    onClose={() => setStarting(false)}
  />
)}
```

- [ ] **Step 3: 빈 상태를 팝업으로 잇는다**

```tsx
<div className="empty-state">
  <strong>수집된 세션이 없습니다</strong>
  <p>위의 <strong>+ 수집 시작</strong>을 눌러 수집 방식을 고르세요.</p>
</div>
```

- [ ] **Step 4: CSS 를 더한다**

```css
/* 수집 시작 팝업 — window.confirm 을 쓰지 않으므로 직접 그린다 */
.modal-backdrop { position: fixed; inset: 0; background: rgba(15, 23, 42, .42);
                  display: flex; align-items: flex-start; justify-content: center;
                  padding: 6vh 16px; z-index: 50; overflow-y: auto; }
.modal-card { width: 100%; max-width: 560px; background: #fff; border-radius: 12px;
              border: 1px solid var(--line); box-shadow: 0 18px 48px rgba(15,23,42,.18); }
.modal-head { display: flex; align-items: flex-start; justify-content: space-between;
              gap: 12px; padding: 16px 18px 12px; border-bottom: 1px solid var(--line); }
.modal-head strong { display: block; font-size: 15px; }
.modal-head span { display: block; font-size: 11.5px; color: var(--muted); margin-top: 2px; }
.modal-head button { border: 0; background: none; font-size: 15px; color: var(--muted);
                     cursor: pointer; padding: 0 2px; }

.engine-picker { display: grid; grid-template-columns: repeat(3, 1fr); gap: 8px; padding: 14px 18px 0; }
.engine-picker button { display: flex; flex-direction: column; align-items: flex-start; gap: 2px;
                        padding: 10px; border: 1px solid var(--line); border-radius: 9px;
                        background: #fff; cursor: pointer; text-align: left; }
.engine-picker button b { font-size: 12.5px; }
.engine-picker button span { font-size: 10.5px; color: var(--muted); line-height: 1.4; }
.engine-picker button.on { border-color: #7cc9b5; background: #f2fbf8; }
.engine-picker button:disabled { opacity: .5; cursor: default; }

.engine-body { padding: 14px 18px 18px; }
.engine-body .crawl-panel { margin: 0; }
```

`.heading-row` 는 이미 `display: flex; justify-content: space-between; align-items:
flex-end` 다 (`app.css:51`, 확인함). 버튼이 오른쪽 아래로 붙으므로 CSS 를 더할 필요가 없다.

- [ ] **Step 5: 타입체크와 눈으로 확인**

Run: `cd apps/admin && npx tsc -b --force` → exit 0

`http://localhost:5173/projects/5` 에서:
- `+ 수집 시작` → 팝업이 열리고 포털이 기본 선택
- 포털 탭에 **드롭다운이 없고** 프로젝트 이름이 고정으로 보인다
- 트래픽 탭에 확장 안내가 프로젝트 이름을 넣어 보인다
- 문서 탭은 비활성
- Esc 와 배경 클릭으로 닫히고, 폼 안쪽 클릭으로는 닫히지 않는다

- [ ] **Step 6: 커밋**

```bash
git add apps/admin/src/components/CollectStartModal.tsx apps/admin/src/pages/SessionList.tsx apps/admin/src/styles/app.css
git commit -m "프로젝트 안에서 수집 방식을 골라 시작한다"
```

---

### Task 5: `/sources` 를 설명 전용으로 격하하고 사이드바 순서를 되돌린다

**Files:**
- Modify: `apps/admin/src/pages/SourceList.tsx`
- Modify: `apps/admin/src/components/Shell.tsx`
- Modify: `apps/admin/src/styles/app.css`

**Interfaces:**
- Consumes: 없음 (제거 작업)
- Produces: 없음
- `GET /api/collection-engines` 와 `/engines/:kind` (`EngineSessionList`) 는 **지운다고
  판단하지 않는다** — 링크만 남겨 도달 가능하게 둔다. 라우트와 백엔드는 그대로다.

- [ ] **Step 1: 폼과 산출물 블록을 뺀다**

`SourceList.tsx` 에서 지운다:
- `<PortalCrawlPanel ... />` 호출 (`244` 근처)
- `<HarvestBlock ... />` 호출 둘 (`211`, `251` 근처)
- `HarvestBlock` 함수 정의 (`131` 근처)
- `harvest` 상태와 `/api/collection-engines` 를 부르는 `useEffect` (`162-170`)
- `projectId`·`projectName` 상태와 프로젝트를 짐작하던 `useEffect` (`172-185`).
  **이 화면은 이제 프로젝트를 모른다** — 프로젝트가 필요한 일이 없기 때문이다.
- `Shell` 에 넘기던 `projectId`·`projectName` prop
- 쓰이지 않게 된 임포트. `useSearchParams` (`2` 행) 는 **`?project=` 초기화에만**
  쓰이므로(`157`, `159` — 확인함) 함께 지운다. `PortalCrawlPanel` 임포트도 지운다.
  `Link` 는 Step 2 에서 쓰므로 **남긴다**

산출물 숫자를 프로젝트 목록으로 옮겼으므로 여기서는 **숫자를 다시 보여주지 않는다.**
범위가 다른 두 숫자(전체 합산 vs 프로젝트 하나)가 어긋나면 버그로 읽힌다.

- [ ] **Step 2: 엔진별 목록으로 가는 링크만 남긴다**

`EngineSessionList` 가 도달 불가가 되지 않도록, 살아 있는 카드 둘의 `Guide` 위에 한 줄을 둔다:

```tsx
{/* 숫자는 프로젝트 목록이 답한다. 여기서는 "이 방식으로 모은 것 전체" 로만 잇는다 */}
<Link className="method-link" to="/engines/traffic">이 방식으로 모은 세션 보기 →</Link>
```

포털 카드는 `/engines/portal`. `Link` 를 임포트한다.

```css
.method-link { display: block; padding: 10px 14px; font-size: 12px; font-weight: 650;
               border-top: 1px dashed var(--line); }
```

- [ ] **Step 3: 카드 높이가 맞는지 확인한다**

폼이 빠지면 세 카드의 내용 높이가 비슷해져 `.method-sources { margin-top: auto }` 로 생긴
공백이 사라진다. `http://localhost:5173/sources` 에서 **왼쪽 카드에 큰 빈 공간이 없는지**
직접 확인한다. 남아 있으면 그때 `margin-top: auto` 를 조정한다 — 미리 고치지 않는다.

- [ ] **Step 4: 사이드바 순서를 되돌린다**

`Shell.tsx` 의 `items` 에서 `수집 엔진` 을 맨 아래로 옮긴다. 프로젝트가 작업의 중심으로
돌아왔고, 엔진은 참고 자료다.

```tsx
const items = [
  { label: "프로젝트", to: "/", prefixes: [] as string[] },
  ...(projectId
    ? [
        { label: "수집 세션", to: `/projects/${projectId}`, prefixes: ["/sessions/", "/spec-sessions/"] },
        { label: "액션", to: `/projects/${projectId}/actions`, prefixes: ["/actions/"] },
        { label: "테스트 콘솔", to: `/projects/${projectId}/console`, prefixes: [] as string[] },
      ]
    : []),
  // 엔진은 장소가 아니라 수집 사건의 속성이다. 참고 자료로 맨 아래 둔다.
  { label: "수집 엔진", to: "/sources", prefixes: ["/engines/"] as string[] },
];
```

`prefixes: ["/engines/"]` 를 더해 엔진별 목록 화면에서도 사이드바가 켜지게 한다.
`isActive` 함수는 그대로 둔다 — `to` 정확 일치 + `prefixes` 접두 일치로 이미 맞다.

- [ ] **Step 5: 타입체크·빌드·눈으로 확인**

```bash
cd apps/admin && npx tsc -b --force && npm run build
```
Expected: 둘 다 성공. `tsc -b` 가 지워진 임포트를 잡아준다.

화면에서 확인한다:
- `/sources` 에 폼과 숫자가 없고, 카드 셋 높이가 맞고, 공백이 없다
- `/engines/portal` 로 갈 수 있고 그 화면에서 사이드바 `수집 엔진` 이 켜져 있다
- 사이드바 맨 위가 `프로젝트`

- [ ] **Step 6: 커밋**

```bash
git add apps/admin/src/pages/SourceList.tsx apps/admin/src/components/Shell.tsx apps/admin/src/styles/app.css
git commit -m "수집 엔진 화면을 설명 전용으로 격하한다"
```

---

### Task 6: 문서와 대본을 바뀐 흐름에 맞춘다

**Files:**
- Modify: `docs/demo-script.md`
- Modify: `README.md`
- Modify: `CLAUDE.md`

**Interfaces:** 없음 (문서)

- [ ] **Step 1: 대본을 고친다**

`docs/demo-script.md` 에서:

- **준비물 목록** (`65-66`): 여는 화면이 `/` (프로젝트 목록) 로 바뀐다.
  `/sources` 는 남기되 "설명용 참고 화면" 으로 적는다
- **장면 0** (`83`): `/sources` 로 시작하는 것을 `/` 로 바꾼다. 대사를 프로젝트 중심으로
  고친다 — 예: "수집한 API 는 프로젝트로 모입니다. 각 프로젝트가 어떤 방식으로 모였는지
  배지로 보입니다". 확인란에 **배지·세션·액션·마지막 수집이 실제 값으로 보이는지**를 적는다
- **장면 0-1** (`84`): 사이드바 `프로젝트` 클릭이 필요 없어졌다. 대신 엔진 셋을 소개하려면
  프로젝트를 열고 `+ 수집 시작` 을 눌러 팝업에서 셋을 보여주는 장면으로 바꾼다.
  사이드바 확인 문구를 **`프로젝트` 가 맨 위**로 고친다 (지금은 `수집 엔진` 이 맨 위라고
  적혀 있다)
- **포털 구간** (장면 11~13): 일괄 수집을 `/sources` 에서 하던 것을 프로젝트 안
  `+ 수집 시작` → 포털 로 바꾼다. **드롭다운이 없다**는 점을 확인란에 적는다
- **리셋 후 상태 체크리스트**: 프로젝트 목록에 배지가 보이는 상태를 반영한다.
  실제로 리셋해 확인한 값만 적는다

- [ ] **Step 2: README 를 고친다**

화면 흐름을 설명하는 절에서 진입 순서를 `프로젝트 → 수집 세션 → 액션 → 콘솔` 로 고치고,
`+ 수집 시작` 팝업에서 수집 방식을 고른다는 것을 적는다. 백엔드 테스트 개수를
`136` 으로 맞춘다 (Task 1 에서 3건 늘었다 — 실제 측정값으로 적는다).

- [ ] **Step 3: CLAUDE.md 에 판단 근거를 남긴다**

"여러 파일을 읽어야 보이는 것" 절에 붙인다:

```markdown
**엔진은 장소가 아니라 수집 사건의 속성이다** (`RecordingSession.kind`). 화면도 그렇게
맞춰져 있다 — 프로젝트가 작업의 중심이고, 수집 시작은 프로젝트 안
`+ 수집 시작` 팝업(`components/CollectStartModal.tsx`)에서 엔진을 골라 그 엔진의
시작 지점으로 간다. **팝업은 세션을 만들지 않는다** — 세션은 실제 수집(확장의 업로드
또는 `CrawlJob`)이 만든다. 빈 세션을 먼저 만드는 흐름을 넣으면 "만들었는데 아무것도
없는 것"이 생기고 `RecordingSession` 에 상태 컬럼이 필요해져 마이그레이션 없는
`dev.db` 가 깨진다.

한때 `수집 엔진`(`/sources`)이 사이드바 맨 위이고 일괄 수집 폼도 거기 있었다.
프로젝트 목록이 **이름·ID·삭제**만 보여줘 수집 과정이 안 보였기 때문인데, 처방을
잘못된 층에 붙인 것이었다. 프로젝트가 필요한 폼이 프로젝트 없는 페이지에 놓여
드롭다운으로 우회했고, 그 폼 때문에 카드 높이가 어긋나 공백이 생겼다. 프로젝트
목록에 엔진 배지를 달아 원래 문제를 풀고 `/sources` 는 설명 전용으로 돌렸다.
**`/sources` 에 산출물 숫자를 다시 넣지 않는다** — 프로젝트 목록 배지와 범위가 달라
(전체 합산 vs 프로젝트 하나) 두 숫자가 어긋나면 버그로 읽힌다.
```

- [ ] **Step 4: 전체 검증**

```bash
cd apps/backend && .venv/bin/pytest tests/ -q      # 136 통과
cd apps/admin   && npx tsc -b --force              # exit 0
cd apps/admin   && npm run build                   # 성공
cd apps/extension && npm run compile && npm test   # 건드리지 않았지만 확인
```

- [ ] **Step 5: 커밋**

```bash
git add docs/demo-script.md README.md CLAUDE.md
git commit -m "바뀐 정보구조를 문서와 대본에 반영한다"
```

---

## Self-Review

**1. 스펙 커버리지** — 승인된 정보구조 표의 다섯 줄이 모두 태스크를 갖는다:

| 표의 줄 | 태스크 |
|---|---|
| 첫 화면 = 프로젝트 목록 | 라우트 변경 없음 (`/` 가 이미 `ProjectList`) |
| 프로젝트 목록 컬럼 | Task 1 (데이터) + Task 2 (화면) |
| 수집 시작 = 프로젝트 안 팝업 | Task 3 (고정 모드) + Task 4 (팝업) |
| `/sources` 격하 | Task 5 |
| 사이드바 순서 | Task 5 |

**2. 플레이스홀더** — 없다. 모든 코드 단계에 실제 코드가 있다. Task 5 Step 3 과
Task 6 Step 1 은 의도적으로 "직접 확인한 값을 쓴다" 로 두었다 — 미리 적으면
전역 제약("화면에 뜨는 숫자는 실제 데이터로 뒷받침")을 어기는 추측이 된다.

**3. 타입 일관성** — Task 1 이 내보내는 `kinds`/`sessions`/`actions`/`lastCollectedAt` 를
Task 2 가 같은 이름으로 읽는다. Task 3 의 "`onProjectChange` 없음 = 고정" 규약을
Task 4 가 `onProjectChange` 를 넘기지 않는 것으로 지킨다. `KindMark`·`KIND_LABEL` 은
`CollectionMark.tsx` 의 실제 export 이름이다(확인함).

**4. 의존 순서** — 1 → 2, 3 → 4 는 지켜야 한다. 5 와 6 은 마지막.
2 와 3 은 서로 독립이다.

## 되돌리는 방법

태스크마다 커밋이 하나씩이라 `git revert` 로 개별 되돌리기가 된다. 백엔드는 Task 1 의
**필드 추가**뿐이므로 스키마 변경이 없고 `dev.db` 를 다시 만들 필요가 없다.
