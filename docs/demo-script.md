# 촬영 대본 — 대회 제출 영상 (3분)

중간결과보고서 §6.4 가 정한 규격을 따른다 — **3분 이내**, 유튜브 일부공개.
보고서의 서사("소스코드를 한 줄도 수정하지 않고", **이중 역분석**)를 화면으로 증명하는 것이
이 영상의 목적이다.

**이 대본은 2026-07-30 dev(`51ea82c`) 기준이다.** UI 가 바뀌면 §확인란부터 어긋난다.
검정 테마·사이드바 6항목·화면 문구 `MCP` 가 이 시점 기준이다.

---

## 0. 촬영 전 준비

### 0-1. DB 리셋 (매 테이크 시작 시)

```bash
rm -f apps/backend/data/dev.db
lsof -ti tcp:8000 -sTCP:LISTEN | xargs kill
cd apps/backend && .venv/bin/uvicorn app.main:app --port 8000
```

`-sTCP:LISTEN` 을 **반드시** 붙인다. 없으면 8000번에 *접속한* 프로세스까지 잡히는데
관리자 화면을 열어 둔 Chrome 이 거기 포함된다 (실측 3개 중 2개가 Chrome). 브라우저가 통째로 닫힌다.

**리셋 직후 상태** — `seed.py` 를 읽어 추론한 값이다. **첫 리허설에서
`curl -s localhost:8000/api/projects` 로 대조해 이 표를 갱신할 것.**

| 항목 | 값 |
|---|---|
| 프로젝트 | 3개 — `국토교통부 실거래가`(#1) / `국가통계포털 KOSIS`(#2) / `공공데이터포털 오픈API`(#3) |
| MCP 도구 | 1개 — `아파트 단지 마커 조회` / `search_apartment_markers` (프로젝트 #1, ACTIVE) |
| 세션 | 1개 — 포털 명세 세션 (프로젝트 #3). #1·#2 는 세션 없음 |
| 배지 | #3 만 포털 배지 — `seed_portal_spec()` 이 그 프로젝트에만 세션을 만든다 |

**프로젝트 번호가 고정이라 장면 7 의 `/projects/1/console` 이 항상 실거래가를 가리킨다.**
리허설로 프로젝트를 하나라도 만든 채 리셋을 건너뛰면 번호가 밀려 다른 프로젝트가 열린다.

### 0-2. 최종 점검 (테이크 직전)

- [ ] 관리자(:5173)·백엔드(:8000) 가 떠 있고 `http://localhost:5173/` 와
      `http://localhost:5173/projects/1/console` 이 오류 없이 열린다
- [ ] 확장을 새로 빌드했거나 `chrome://extensions` 에서 새로고침했다면
      **대상 페이지도 새로고침**한다. 이미 열려 있던 탭의 스크립트는 고아가 되어
      클릭이 하나도 잡히지 않는데, 화면에는 오류 없이 "0 클릭"만 보여 알아채기 어렵다
- [ ] `rt.molit.go.kr` 지도가 열리고 확대 버튼이 눌린다
- [ ] **`apps/backend/.env` 를 화면에 띄우지 않는다** — Azure 키가 들어 있다
- [ ] `docs/report/assets/` 캡처를 다시 딸 계획이면 이 테이크에서 함께 딴다
      (지금 보고서 캡처는 **옛 밝은 테마**라 영상과 다른 제품처럼 보인다)

### 0-3. 미리 열어 둘 탭 (3분을 맞추려면 필수)

| 탭 | 주소 |
|---|---|
| 1 | `http://localhost:5173/` |
| 2 | `https://rt.molit.go.kr/pt/gis/gis.do` |
| 3 | `http://localhost:5173/projects/3` (공공데이터포털 오픈API) |

탭 이동은 편집으로 잘라낸다. **주소를 타이핑하는 장면은 넣지 않는다.**

### 0-4. 인증키 — 없는 것을 있는 척하지 않는다

`.env` 의 `DATA_GO_KR_SERVICE_KEY` 가 **비어 있다.** 포털·문서 경로로 만든 MCP 도구는
**실행이 막힌다** — 수집과 도구 생성까지만 보여준다.

이걸 감추지 않고 **안전장치로** 말한다. Playground 헤더에 `인증키 없음` 칩이 뜨고,
보고서 §3.3.4 의 "키가 없으면 호출 전에 차단"이 화면으로 증명된다.

키를 발급받아 `.env` 에 넣고 백엔드를 재시작하면 `seed_credentials()` 가 자동으로 채운다.
그때는 장면 14 를 실제 호출까지 늘릴 수 있다.

---

## 장면별 대본 (합계 180초)

| # | 장면 | 조작 | 대사 (요지) | 초 | 확인 포인트 |
|---|---|---|---|---|---|
| 1 | 문제 제기 | 탭 2 — 실거래가 지도 | "이 지도는 API 로 데이터를 받아옵니다. 그 API 의 명세는 어디에도 공개돼 있지 않습니다. AI 에게 쓰게 하려면 사람이 뜯어봐야 했습니다" | 10 | 지도가 그려져 있다. 개발자 도구는 열지 않는다 |
| 2 | 기록 시작 | 확장 아이콘 → 사이드 패널 → 프로젝트 이름 `국토교통부 실거래가` → **기록 시작** | "브라우저 확장이 화면이 부르는 호출을 관측합니다. 대상 소스코드는 건드리지 않습니다" | 12 | 이름을 **정확히** 입력한다 — 같은 이름이면 시드 프로젝트(#1)에 담긴다 |
| 3 | 클릭 한 번 | 지도의 **확대(+) 버튼을 한 번** 클릭 | "확대 한 번에 백엔드 호출 세 건이 잡힙니다" | 10 | **드래그하지 않는다.** 드래그로 생긴 요청은 `interactionId` 가 `null` 이 되어 클릭과 묶이지 않는다 (리허설 재확인) |
| 4 | 전송 | **기록 종료 및 전송** → **관리자에서 열기** | (조작만) | 6 | 패널에 요청이 3건 안팎 쌓인 것을 보여준 뒤 누른다 |
| 5 | 후보 점수 | 세션 상세 | "화면의 데이터를 실제로 나른 API 가 위로 옵니다. 로그성 호출은 감점돼 맨 아래입니다" | 12 | `accesLog.do` 가 최하위. **행을 지목할 때 순서가 아니라 URL 로 말한다** — `getMarker.do` 와 `getCenterLedCdPnu.do` 는 동점(9)이라 순서가 고정되지 않는다 |
| 6 | MCP 도구 생성 | `getMarker.do` 행의 **MCP 만들기** | "파라미터 여섯 개가 타입과 실제 예시값까지 자동으로 채워집니다. 문서를 읽은 게 아니라 실제로 오간 값에서 추론한 것입니다" | 15 | 경위도 4·연도·종류 코드. 오른쪽 ActionSpec 에 **보존 헤더**(`Referer`·`X-Requested-With`)가 보인다. **버튼 활성 여부를 약속하지 않는다** — 비활성 조건은 점수가 아니라 `!isJson` 이다 |
| 7 | 자연어 실행 | 사이드바 **06 Playground** → `광화문 근처 아파트 단지 알려줘` | "이제 사람 말로 부릅니다" | 10 | 질의는 미리 클립보드에 담아 붙여넣는다 |
| 8 | 실행 결과 | (관찰) | "LLM 이 도구를 고르고 인자 여섯 개를 채웠습니다. 실제 API 를 호출해 돌아온 데이터로 답합니다" | 18 | **HTTP 200**(실측 229ms). 오른쪽 호출 로그에 `INPUT / REQUEST / OUTPUT`. 실제 단지명이 나온다. **"몇 초 안에" 같은 시간을 약속하지 않는다** — 실측 편차 152ms~5,380ms |
| — | **여기까지 93초 — 주 시나리오 끝** | | | | |
| 9 | 둘째 경로 | 탭 3 → **+ 수집 시작** → **포털 공개 기반** | "명세만 게시하고 실행은 못 하는 포털은 관측으로 잡히지 않습니다. 그래서 공개된 명세를 읽습니다" | 10 | 팝업에 **프로젝트가 고정 표시**되고 드롭다운이 없다 |
| 10 | 자연어로 API 고르기 | 용도란에 `지역별 실시간 대기질과 미세먼지 예보를 알려주는 챗봇` → **API 찾기** | "쓸 용도를 적으면 포털을 검색해 목적에 맞는 것만 골라 체크합니다. AI 가 쓸 도구를 AI 가 고릅니다" | 18 | 후보마다 **고른 이유·제외한 이유가 한 줄씩** 붙는다. 팝업이 겹치지 않고 같은 자리에서 이어진다 |
| 11 | 서버에서 돈다 | **수집 시작** → 자동으로 **04 수집 진행현황** | "수집은 서버에서 돕니다. 창을 닫아도 계속됩니다" | 10 | 진행 막대와 `수집한 API · 확인한 서비스 · 발견한 서비스` 가 오른다 |
| 12 | 셋째 경로 | **02 API 수집하기** → **문서 기반** → `examples/documents/01_기상청_단기예보조회서비스_활용가이드.pdf` 드래그 | "세 번째는 문서입니다. 활용가이드 PDF 를 올리면 LLM 이 명세를 구조화합니다" | 12 | 드롭 영역에 담긴 파일이 보인다 |
| 13 | 지어내지 않는다 | 이어서 `03_보도자료_공공데이터_개방계획.pdf` 를 함께 올려 결과 비교 | "보도자료에는 엔드포인트가 없습니다. 그런 문서에서는 도구를 만들지 않습니다 — 없는 API 를 지어내지 않는 것이 더 중요합니다" | 15 | 활용가이드는 오퍼레이션이 나오고, 보도자료는 **만들지 않은 이유가 표시**된다 |
| 14 | 인증키 게이트 | **06 Playground** (포털 프로젝트) | "포털 API 는 인증키가 필요합니다. 키는 LLM 에게 넘기지 않고 서버가 실행 직전에 채웁니다. 등록되지 않았으면 호출 전에 막습니다" | 10 | 헤더에 `인증키 없음` 칩. **약점이 아니라 설계임을 말로 분명히 한다** |
| 15 | 마무리 | 탭 1 (프로젝트 목록) | "세 경로로 모은 것이 하나의 레지스트리로 합류합니다. 이후 도구 변환과 실행은 수집 방식을 구분하지 않습니다" | 12 | 카드마다 수집 방식 배지와 MCP 도구 수가 보인다 |

**합계 180초.** 편집으로 탭 이동·로딩을 잘라내면 맞는다.

### 자막 (보고서 §6.4 요구)

마지막 5초에 겹쳐 넣는다:

```
프로토타입 범위 — 로그인·권한 관리·MCP 표준 엔드포인트는 2차 기간 계획입니다.
포털 인증키가 등록되지 않은 상태라 포털·문서 경로는 도구 생성까지만 보여줍니다.
```

---

## 대사에서 쓰지 말아야 할 말

화면과 어긋나면 심사위원이 먼저 알아챈다. `#33` 에서 화면 문구가 `액션` → `MCP` 로 바뀌었다.

| 쓰지 않는다 | 지금 이름 |
|---|---|
| 액션 | **MCP 도구** |
| 테스트 콘솔 | **Playground** |
| 수집 세션 | **수집현황** |
| 수집 엔진 | **수집 방식 안내** (왼쪽 레일 아이콘) |

**약속하지 않는 것 셋** — 실측이 흔들리는 값이다.

1. **소요 시간.** LLM 요약이 전체 시간을 지배하고 편차가 크다 (152ms ~ 5,380ms)
2. **후보 순위.** `getMarker.do` 와 `getCenterLedCdPnu.do` 가 동점이라 순서가 고정되지 않는다
3. **버튼 활성 여부.** `MCP 만들기` 의 비활성 조건은 점수가 아니라 응답이 JSON 이 아닐 때다

---

## 실측 기록

**재보지 않은 값을 대사에 넣지 않는다.**

| 항목 | 실측 | 측정일 |
|---|---|---|
| Playground 실행 | `search_apartment_markers` 선택, 파라미터 6개 자동 완성, **HTTP 200 · 229ms**, 실제 단지명 반환 | 2026-07-30 |
| 포털 일괄 수집 | 검색 URL 1건 → 92초에 서비스 16개·오퍼레이션 30개 | 2026-07-30 (`docs/report/assets/metrics.md`) |
| 트래픽 기록 | 지도 확대 1클릭당 요청 3건 안팎 | README §4-1 |
| LLM 응답 편차 | 152ms ~ 5,380ms | 리허설 기록 |
| WAF | `PRESERVED_HEADERS`(Referer·X-Requested-With·Accept·User-Agent·Content-Type)를 그대로 재현하면 막히지 않았다. 하나라도 빠지면 400 이 재현될 수 있다 | 리허설 기록 |

**장면 10·11·12·13 은 아직 실측하지 않았다.** 자연어 API 선택의 소요와 문서 수집의 결과
건수를 첫 리허설에서 재서 위 표에 적는다.

촬영 당일 400 이 나면 먼저 볼 곳은 `services/schema_infer.py` 의 `PRESERVED_HEADERS` 다.
`services/executor.py` 는 거기에 User-Agent·Accept 기본값만 채운다.

---

## 재촬영 체크리스트

| 다시 찍을 장면 | 되돌릴 범위 |
|---|---|
| 1~4 (기록) | **§0-1 리셋 필수** — 세션 번호가 밀리고 중복 도구가 생긴다 |
| 5~6 (후보·도구 생성) | 리셋 필수 — 도구를 이미 만들었으면 같은 이름이 둘 생긴다 |
| 7~8 (Playground) | 리셋 불필요. 질의만 다시 던진다 |
| 9~11 (포털) | 리셋 불필요. 다만 같은 URL 로 다시 수집하면 **직전 세션에 누적**된다(설계) |
| 12~13 (문서) | 리셋 불필요. 같은 PDF 를 다시 올리면 세션이 하나 더 생긴다 |
| 14~15 (마무리) | 리셋 불필요 |

**리셋했으면 확장도 새로고침하고 대상 페이지도 새로고침한다** (§0-2 둘째 항목).

---

## 감지가 실패하면

장면 9~11 은 `data.go.kr` 의 화면 구조에 의존한다. 포털이 바뀌면 검색·파싱이 실패한다.
테이크 직전에 한 번 돌려 보고, 실패하면 그 구간을 빼고 **장면 12~13(문서 경로)을 늘린다.**
문서 경로는 저장소 안의 PDF 만 쓰므로 외부 변수가 없다. 그때 장면 15 의 대사를
"두 경로로 모은 것이" 로 줄인다.

확장의 단건 명세 수집(`공개 명세 페이지 감지`)을 대신 쓸 수도 있다. 판정 기준은 페이지에
`요청주소` 가 있고 `요청변수` 표가 함께 있는 것이다 (`apps/extension/lib/spec-detect.ts`).

---

## 시연용 문서 4개

`examples/documents/` 에 있다. 성공 2개와 실패 2개가 **의도적으로** 섞여 있다 —
장면 13 의 "지어내지 않는다"를 증명하는 세트다.

| 파일 | 기대 결과 |
|---|---|
| `01_기상청_단기예보조회서비스_활용가이드.pdf` | 오퍼레이션이 나온다 |
| `02_사내_회의실예약_API_명세서.pdf` | 사내 시스템도 된다 — B2B 서사에 쓸 수 있다 |
| `03_보도자료_공공데이터_개방계획.pdf` | 엔드포인트가 없어 만들지 않는다 |
| `04_국토교통부_건축물대장_활용가이드(스캔본).pdf` | 텍스트 층이 없어 읽지 못한다 |

**넷 다 아직 실측하지 않았다.** 리허설에서 각각 올려 보고 기대 결과가 맞는지 확인한 뒤
장면 12·13 에 쓸 두 개를 확정한다.
