# 촬영 대본 — mcp-studio 데모

> 이 데모는 **프로토타입**입니다. 프로젝트 CRUD·로그인·액션 버전 관리 등은 생략되어 있습니다.

이 화면 표기는 촬영본 자막이나 인트로 자막에 반드시 포함한다.

---

## 0. 촬영 시작 전 — DB 초기화 (매 촬영 시작 시, 재촬영 시작 시 모두 실행)

세션 번호를 `#1`부터 다시 보여주고, 리허설 중 쌓인 세션·액션을 지워 화면을
깨끗한 상태로 되돌리기 위해 **아래 절차로 막는다.**

**아래 절차는 최초 촬영 시작 전뿐 아니라, NG 이후 재촬영(re-take)을 시작하는
시점마다 실행한다.** "한 번만 하면 되는 준비 작업"이 아니라 매 테이크의 리셋 버튼이다.

```bash
# 1) DB 파일 자체를 지운다 — 세션 번호가 #1부터 다시 시작되고 중복 액션이 사라진다
rm -f apps/backend/data/dev.db

# 2) 포트를 쥐고 있는 낡은 프로세스를 확실히 죽인다.
#    pid 파일(/tmp/backend.pid)에 의존하지 않는다 — 파일이 가리키는 프로세스가
#    이미 죽었거나 다른 프로세스로 바뀌어 있을 수 있고, 그러면 이전 커밋의
#    코드를 그대로 물고 있는 uvicorn이 8000번 포트에 남아 조용히 낡은 라우트를
#    서빙한다(콘솔이 404를 내는 원인이었다). lsof로 실제 점유 프로세스를 찾아 죽인다.
#
#    -sTCP:LISTEN 을 반드시 붙인다. 이게 없으면 lsof가 8000번에 "접속한" 쪽까지
#    함께 돌려주는데, 거기에는 관리자 화면을 열어둔 Chrome이 포함된다.
#    실측에서 3개가 잡혔고 그중 둘이 Chrome이었다 — 촬영 중에 실행하면
#    브라우저가 통째로 닫힌다.
lsof -ti tcp:8000 -sTCP:LISTEN | xargs -r kill

# 3) 백엔드를 새로 기동한다 — 기동 시 init_db() + seed()가 실행되어
#    테이블 생성과 시드 데이터 삽입이 자동으로 끝난다
cd apps/backend && .venv/bin/uvicorn app.main:app --port 8000
```

세션 번호가 `#1`이 되어 화면에 자연스럽게 나오는 부수 효과도 있다 — 이 점을
장면 4→5 전환에서 대사로 활용해도 된다.

### 촬영 전 최종 점검 (Pre-flight, 매 테이크 직전)

1. **백엔드가 최신 코드를 서빙하는지 확인한다.** pid나 로그를 신뢰하지 말고
   라우트 존재 자체를 확인한다:
   ```bash
   curl -s http://localhost:8000/openapi.json | python3 -c \
     "import json,sys; print('/api/projects/{project_id}/llm-test' in json.load(sys.stdin)['paths'])"
   ```
   `True`가 아니면 위 §0의 2)~3)을 다시 수행한다 — 낡은 프로세스가 여전히
   포트를 쥐고 있다는 뜻이다.

2. **`.env`가 로드되는지 확인한다.** 값은 화면(카메라)에 절대 띄우지 않고
   네 개의 키가 "있다/없다"만 확인한다:
   ```bash
   cd apps/backend && .venv/bin/python -c \
     "from dotenv import load_dotenv; import os, pathlib; \
      load_dotenv(pathlib.Path('.env')); \
      keys=['AZURE_OPENAI_API_KEY','AZURE_OPENAI_ENDPOINT','AZURE_OPENAI_API_VERSION','AZURE_OPENAI_DEPLOYMENT']; \
      print({k: bool(os.environ.get(k)) for k in keys})"
   ```
   네 값이 모두 `True`여야 한다. `apps/backend/.env`는 git-ignore 대상이며
   **어떤 경우에도 커밋하지 않는다.**

3. 관리자 화면(`apps/admin`, 5173번 포트)이 떠 있는지, 아래 셋이 에러 없이 열리는지 확인한다.
   - `http://localhost:5173/` (프로젝트 목록 — 장면 0에서 여는 화면)
   - `http://localhost:5173/sources` (수집 엔진 — 설명 전용 참고 화면, 장면 0-1에서 잠깐 다룸)
   - `http://localhost:5173/projects/1/console` (테스트 콘솔)

4. **확장을 새로 빌드했거나 `chrome://extensions`에서 새로고침했다면, 대상 페이지도
   반드시 새로고침한다.** 이미 열려 있던 탭의 콘텐츠 스크립트는 고아가 되어 클릭이
   하나도 잡히지 않는다. 화면에는 오류 없이 "0 클릭"만 보이므로 촬영 중에는
   알아채기 어렵다.

5. 실거래가 사이트(`https://rt.molit.go.kr`)와 Azure OpenAI 호출은 모두 **실측·과금
   대상**이다. 리허설 중 반복 호출을 최소화하고, 최종 테이크 직전 1회만 정식으로 돌린다.

---

## 장면별 대본

| # | 장면 | 조작 | 대사 (요지) | 예상 소요 | 확인 포인트 |
| - | --- | --- | --- | --- | --- |
| 0 | 프로젝트 목록 소개 | 브라우저에서 `http://localhost:5173/` (첫 화면) 열기 | "수집한 API 는 프로젝트로 모입니다. 각 프로젝트가 어떤 방식으로 모였는지 배지로 보입니다" | 8초 | 프로젝트마다 **배지·수집 세션·액션·마지막 수집이 실제 값으로** 다르게 보인다 — 세션이 없는 프로젝트는 배지가 없고 마지막 수집이 `—` 다. 테이크 직전에 `curl -s localhost:8000/api/projects` 로 실제 값을 확인해 화면과 대조한다. **개수는 대사에 넣지 않는다** — §0 리셋 여부에 따라 달라진다 |
| 0-1 | 수집 방식 셋 소개 | 프로젝트 하나를 열고 **`+ 수집 시작`** 클릭 → 팝업에서 엔진 셋 확인 | "API 를 가져오는 방식은 셋입니다. 화면이 부르는 호출을 관측하거나, 포털이 공개한 명세를 읽거나, 문서를 올립니다. 어느 방식이든 이후 액션 생성과 테스트는 같습니다" | 6초 | 팝업 안 엔진 선택 카드 셋(트래픽/포털/문서)이 보이고 문서는 비활성으로 구분돼 있다. **이 팝업은 세션을 만들지 않는다** — 닫아도 세션 목록이 늘지 않는지 확인. 사이드바 확인: **`프로젝트`가 맨 위**이고(지금은 `수집 엔진`이 맨 위가 아니다), `수집 엔진`은 맨 아래에 있다 |
| 1 | Side Panel에서 프로젝트 이름 입력 후 "기록 시작" | 확장 아이콘 → Side Panel 열기 → 프로젝트 이름란에 "국토교통부 실거래가" 입력 → 기록 시작 | "타겟 사이트에서 기록을 시작합니다" | 5초 | 세션 번호가 `#1`로 보이는지 (§0 리셋 직후에만 보장됨) |
| 2 | 실거래가 지도를 광화문으로 이동 | 지도 확대/축소 버튼을 **클릭**한다 | "지도를 광화문 쪽으로 이동합니다" | 10초 | **드래그하지 않는다.** 지도 드래그로 발생한 요청은 `interactionId`가 `null`이 되어 클릭에 연결되지 않는다. 확대/축소 버튼 클릭만 상관관계가 잡힌다. |
| 3 | Side Panel에 클릭과 API 요청 3건이 실시간으로 쌓임 | (조작 없음, 관찰) | "클릭 하나에 백엔드 요청 3개가 잡히는 걸 볼 수 있습니다" | 10초 | `POST 200 /pt/gis/getMarker.do`가 하단에 표시됨 |
| 4 | "기록 종료" → 서버 전송 → Side Panel에 "관리자에서 열기" 등장 | Side Panel에서 기록 종료 클릭 | "기록을 종료하면 서버로 전송됩니다. 이제 이 버튼을 누르면 방금 만든 세션이 관리자 화면에서 바로 열립니다" | 5초 | 전송 완료 후 **관리자에서 열기** 버튼이 나타나는지 |
| 5 | 관리자 화면 기록 세션 상세 | Side Panel의 **관리자에서 열기** 클릭 (새 탭으로 `/sessions/<id>`가 열림) | "클릭별로 후보 API가 점수순으로 정렬됩니다" | 15초 | **행을 "몇 번째 줄"로 지목하지 않는다.** 요청 목록에는 `ORDER BY`가 없어 동점 요청은 실행할 때마다 순서가 바뀔 수 있다. `getMarker.do`와 `getCenterLedCdPnu.do`는 둘 다 ★9로 동점이며, 화면에서는 URL로 대상 행을 짚는다 — "여기, `getMarker.do` 요청을 보시면"처럼. `accesLog.do`(사유에 "로그 API -5")는 항상 최하위다 — **"액션 만들기" 버튼의 활성/비활성 여부는 대사로 말하지 않는다.** 이 버튼은 점수가 아니라 응답이 JSON인지(`isJson`)로 결정되며, 로그 API라도 JSON을 반환하면 활성 상태다. 당일 녹화 데이터에 따라 갈리므로 "점수가 낮아 비활성화되어 있습니다" 같은 대사는 넣지 않는다. 점수와 사유만 짚는다. 참고: `docs/screenshots/scene5-session-detail.png` |
| 6 | `getMarker.do` 선택 → 액션 편집 화면 | `getMarker.do` 행의 "액션 만들기" 클릭 | "이 요청을 액션으로 등록합니다" | 5초 | `/actions/new?requestId=N`이 Action을 한 번만 생성하고 곧바로 `/actions/:id`로 이동한다 — 주소창에 실제 액션 id가 보이는지 |
| 7 | 파라미터 6개가 자동 추론되어 있음. 설명 입력 후 활성화 | `minX/minY/maxX/maxY/srhYear/poiType` 설명란에 한국어 설명 입력 → "활성화하고 테스트하기" | "LLM이 각 파라미터를 이해하도록 설명을 채워줍니다" | 20초 | 예시값 컬럼에 실제 기록값(126.9654155 / 37.5606793 / 126.9911647 / 37.5720409 / 2026 / "A")이 그대로 보이는지 |
| 8 | LLM 테스트 콘솔에 질의 입력 | `/projects/1/console` 이동 → "광화문 근처 아파트 단지 알려줘" 입력 → 질의 | "자연어로 물어보면 LLM이 적절한 도구를 고릅니다" | 5초 (호출은 별도) | 참고: `docs/screenshots/scene8-llm-console.png`. 세션·액션이 미리 시드되어 있으므로 이 장면은 §0 리셋 직후에도 바로 재현 가능하다 |
| 9 | 선택된 Tool과 파라미터가 표시됨 → 확인 → 실행 | "이 내용으로 실행" 클릭 | "선택된 도구와 인자를 확인하고 실행합니다" | 대기 시간은 아래 참고 | `selectedTool`이 `search_apartment_markers`인지, 인자에 위경도 4개·연도·poiType이 모두 채워졌는지 |
| 10 | 실제 단지 목록 JSON이 반환되고 요약이 표시됨 | (조작 없음, 관찰) | "실제 API가 호출되어 경희궁자이, 광화문스페이스본 같은 실제 단지가 반환됩니다" | 대기 시간은 아래 참고 | HTTP 200과 한국어 요약 문장이 함께 뜨는지 |

### 포털 공개 기반 수집 (장면 11~13, 약 40초)

장면 0-1 에서 수집 방식이 셋이라고 말했으므로, 둘째 방식도 짧게 보여준다.
보여주지 않으면 "왜 하나만 되나" 로 읽힌다.

**이 구간은 액션 생성에서 끝낸다.** 실행에는 포털에서 발급받은 `serviceKey` 가
필요하고, 그 발급은 제품 밖의 절차다. 없는 것을 있는 척하지 않는다.

| # | 장면 | 조작 | 대사 (요지) | 예상 소요 | 확인 포인트 |
| - | --- | --- | --- | --- | --- |
| 11 | 포털 명세 페이지에서 감지 | 새 탭에서 `https://www.data.go.kr/data/15073861/openapi.do` 열기 → 확장 사이드 패널 | "공공데이터포털처럼 **API 를 실행하지 않고 명세만 게시하는** 사이트는 트래픽을 관측해도 잡히지 않습니다. 이때는 페이지에 실린 명세 표를 직접 읽습니다" | 12초 | 패널에 **공개 명세 페이지 감지** 와 상세기능·요청변수 개수가 뜬다. 주 버튼이 `공개 명세 수집` 으로 바뀐다 |
| 12 | 수집 → 관리자에서 열기 | 프로젝트 이름 입력 → **공개 명세 수집** → **관리자에서 열기** | "명세를 읽어 오퍼레이션으로 만듭니다" | 12초 | 스테퍼 라벨이 트래픽과 다르다(**페이지 열기 / 명세 감지 / 명세 파싱**). 표 위에 "상세페이지는 상세기능을 목록에서 하나씩 보여줍니다" 안내가 함께 뜬다 — 한 번에 하나만 잡히는 것이 버그가 아님을 밝히는 문구다. **참고:** 여러 서비스를 목록 URL 하나로 한꺼번에 모으는 일괄 수집(README §4-6)을 대신 시연하려면 예전처럼 `/sources` 가 아니라 프로젝트 안 **`+ 수집 시작` → 포털**을 누른다 — 프로젝트가 이미 고정돼 있어 **프로젝트 드롭다운이 없다** |
| 13 | 액션 생성 — 인증키는 LLM 에게 숨긴다 | 오퍼레이션의 **액션 만들기** | "같은 액션 편집 화면입니다. 트래픽으로 모았든 명세로 읽었든 이후는 같습니다. 다만 `serviceKey` 는 LLM 에게 넘기지 않고 실행 직전에 서버가 채웁니다 — 키가 없으면 호출 전에 막습니다" | 16초 | 파라미터 표에 `serviceKey` 가 잡혀 있다. **화면에는 "LLM 에게 숨긴다"는 표시가 없다** — 오른쪽 ActionSpec 패널의 `"llmEditable": false` 와 `"authMode": "CREDENTIAL"` 을 짚어 말한다 |

**11번 전에 반드시 확인한다.** 포털 페이지의 구조가 바뀌면 감지가 실패한다.
판정 기준은 페이지에 `요청주소` 가 있고 `요청변수` 표가 함께 있는 것이다
(`apps/extension/lib/spec-detect.ts`). 테이크 직전에 그 페이지를 열어 패널에
**공개 명세 페이지 감지** 가 뜨는지 눈으로 보고 들어간다. 뜨지 않으면 이 구간을
빼고 장면 0-1 의 대사만 "포털·문서 방식도 같은 파이프라인을 씁니다" 로 줄인다.

---

**8~10번 소요 시간에 대한 주의:** 실측으로 5380ms와 152ms 두 값이 모두 관찰됐다.
LLM 요약 생성 단계가 전체 시간을 지배하며 매 호출 편차가 크다. **대본에서
"몇 초 안에 결과가 나옵니다" 같은 특정 시간을 약속하지 않는다.** "실시간으로
응답을 기다립니다" 정도로만 말하고, 실제 대기는 편집 단계에서 컷 처리한다.

---

## 리허설 기록 (Step 3 실측)

전체 흐름(장면 1~10)을 리셋된 DB에서 처음부터 한 번 완주하며 다음을 확인했다:

- 지도 확대/축소 버튼 클릭은 `interactionId`로 정상 연결된다. 드래그는 연결되지
  않는 것이 재확인됨 — 촬영 시 반드시 클릭/버튼 조작만 사용한다.
- 세션 상세(장면 5)에서 `accesLog.do`는 항상 최하위로 나타난다.
  `getMarker.do`(★9)와 `getCenterLedCdPnu.do`(9)는 동점이라 순서가 고정되지
  않으므로, 화면에서 행을 지목할 때는 순서가 아니라 URL로 말한다.
- "액션 만들기" 버튼의 비활성 조건은 **점수가 아니라 응답이 JSON이 아닐 때**
  (`disabled={!c.isJson}`)다. 로그 API 감점(-5)과는 무관하다. 검증용 합성
  데이터로는 이 행이 JSON을 반환해 활성으로 보였고, 실제 녹화에서는 비활성으로
  보인 적이 있다 — 둘 다 정상이다. 대본에서 버튼 상태를 약속하지 않는다.
- 대상 사이트(WAF)는 `PRESERVED_HEADERS`(Referer, X-Requested-With, Accept,
  User-Agent, Content-Type)를 그대로 재현하면 막히지 않았다. 헤더가 하나라도
  빠지면 400이 재현될 수 있다. 헤더를 골라 ActionSpec에 저장하는 쪽은
  `apps/backend/app/services/schema_infer.py`의 `PRESERVED_HEADERS`이고,
  `apps/backend/app/services/executor.py`는 거기에 User-Agent·Accept 기본값만
  채운다 — 촬영 당일 400이 나면 먼저 볼 곳은 schema_infer 쪽이다.

---

## 재촬영(re-take) 시 체크리스트

NG가 나서 특정 장면부터 다시 찍을 때, 어느 장면부터 다시 시작하든
**§0의 DB 초기화 3단계를 먼저 실행한다.** 특히:

- 장면 1(기록 시작)부터 다시 찍으면 새 기록 세션이 하나 더 쌓인다 — §0을
  건너뛰고 이어서 찍으면 관리자 화면의 세션 목록·번호가 리허설 흔적을 그대로
  보여준다.
- §0을 실행하면 항상 같은 상태로 돌아온다. 리허설 중 만든 프로젝트·세션·액션은
  전부 사라지므로, 화면에 테스트 흔적이 노출될 일이 없다.

  | 항목 | 리셋 직후 |
  | --- | --- |
  | 프로젝트 | 3개 — 국토교통부 실거래가(#1) / 국가통계포털 KOSIS(#2) / 공공데이터포털 오픈API(#3) |
  | 배지 | #1·#2 는 배지 없음(수집 세션 없음). **#3 만** 포털 배지 — `seed_portal_spec()`이 이 프로젝트에만 `kind="portal"` `RecordingSession`을 만든다 |
  | 액션 | 1개 — "아파트 단지 마커 조회" (프로젝트 #1, ACTIVE) |
  | 세션 | 1개 — 포털 명세 세션 (프로젝트 #3). #1·#2 는 세션 없음 |

  **주의: 이 표는 실제로 리셋을 실행해 측정한 값이 아니라 `apps/backend/app/seed.py`
  코드를 읽어 추론한 값이다.** 이번 문서 작업 시점에는 프로젝트 #5("포털 일괄 수집
  테스트", 포털 액션 10개 — 인증키 테스트용으로 남겨 두라는 지시가 있었다)를 보존해야
  해서 §0 리셋을 실행하지 않았다. 실측이 필요하면 다음 촬영 전 리허설에서 §0을 실행한
  직후 `curl -s localhost:8000/api/projects`로 이 표를 갱신한다.

  프로젝트 번호가 고정이라 장면 8의 `/projects/1/console` 이 항상 실거래가를
  가리킨다. **리허설로 프로젝트를 하나라도 만든 채 §0을 건너뛰면 번호가 밀려
  이 주소가 다른 프로젝트를 열게 된다** — 리셋을 건너뛰지 않는 이유 중 하나다.
